# TuteDude
TuteDude Test

Git & GitHub
 * For first Time need to configure github in out system
    1. git config --global user.name "Dipakk**"
    2. git config --global user.email "dipak**********32@mail.com"
    
    Optional command
     git config --global color.ui auto

    Mandatory Command
    1. git init
    2. git remote -v
    2.a. git remote add origin https://github.com/username/repository-name.git

    3. git pull origin main
    4. git status
    5. git add .
    6. git commit -m "message"
    7. git push
