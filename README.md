# TuteDude
TuteDude Text

Git & GitHub

 * Cloneing git or Downlode
   1. git coln "url of repository"

 * For first Time need to configure github in out system
    1. git config --global user.name "Dipakk**"
    2. git config --global user.email "dipak**********32@mail.com"
    
 * Optional command
      1. git config --global color.ui auto

 * Mandatory Command
    1. git init
    2. (a) git remote -v
    2. (b) git remote add origin https://github.com/username/repository-name.git

 * Check, Uplode and update
    1. git pull origin main
    2. git status
    3. git add .
    4. git commit -m "message"
    5. git push

 * Make Branch
   1. git checkout -b "branchname"
      - Switch Branch
         1. git checkout "branchname"
   